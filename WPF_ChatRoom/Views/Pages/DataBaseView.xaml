<UserControl x:Class="WPF_ChatRoom.Views.Pages.DataBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_ChatRoom.Views.Pages"
             xmlns:cc = "clr-namespace:WPF_ChatRoom.CustomControl"
             mc:Ignorable="d" 
             d:DesignHeight="678" d:DesignWidth="1178">

    <Border Background="WhiteSmoke" CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!--页头-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.ColumnSpan="99" Text="数据库管理" Width="300" Height="80" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <cc:CustomButton Grid.Row="1" Content="刷新" Command="{Binding RefreshCommand}" Width="80" Height="30" Background="Orange" Style="{StaticResource CustomButtonStyle}" /> 
                <cc:CustomButton Grid.Row="1" Grid.Column="1" Content="删除选中" Command="{Binding DeleteSelectCommand}" Width="80" Height="30" Background="Red" Style="{StaticResource CustomButtonStyle}" /> 
            </Grid>
            
            <!--数据表-->
            <DataGrid Grid.Row="1" IsReadOnly="True" ItemsSource="{Binding UI_Messages}" SelectedItem="{Binding SelectedItem }"/>
            
            <!--页尾-->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
               
                <StackPanel Grid.ColumnSpan="99" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    
                    <TextBlock Text="EndPoint:" Height="25" Width="80" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    <TextBox Grid.Column="1" Width="Auto" Text="{Binding EndPoint}"/>
                
                    <TextBlock Grid.Column="2" Text="Content:" Height="25" Width="80" VerticalAlignment="Center" HorizontalAlignment="Left"/> 
                    <TextBox Grid.Column="3" Width="Auto" Text="{Binding Content}"/>
                </StackPanel>
                
                <RadioButton Grid.Row="1" Grid.ColumnSpan="99" Content="是否自己" HorizontalAlignment="Center"/>
                

                <cc:CustomButton Grid.Row="2" Content="添加" Command="{Binding AddCommand}" Width="80" HorizontalAlignment="Right" Style="{StaticResource CustomButtonStyle}" />
                <cc:CustomButton Grid.Row="2" Grid.Column="1" Content="保存修改" Command="{Binding EditCommand}" Width="80" HorizontalAlignment="Left" Style="{StaticResource CustomButtonStyle}" />
            </Grid>
        </Grid>
    </Border> 
</UserControl>
