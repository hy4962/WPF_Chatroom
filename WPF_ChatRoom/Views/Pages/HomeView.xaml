<UserControl x:Class="WPF_ChatRoom.Views.Pages.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_ChatRoom.Views.Pages"
             xmlns:custom="clr-namespace:WPF_ChatRoom.CustomControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="WhiteSmoke" CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <!-- 消息列表 -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <!-- 使用ItemsControl显示消息列表 注意 此时绑定的是Messages集合 里面每一项都是一个chatmessage对象 此时ItemsControl内部的数据上下文就变成了Messages -->
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- 根据消息类型左右对齐 -->
                            <StackPanel HorizontalAlignment="{Binding Alignment}">
                                <Border Background="{Binding BubbleColor}"
                                    CornerRadius="10"
                                    Padding="12,8"
                                    MaxWidth="300"
                                    Margin="5">
                                    <TextBlock Text="{Binding Content}"
                                           TextWrapping="Wrap"
                                           Foreground="White"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 输入框 -->
            <Grid Grid.Row="2" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="1*"/>

                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding InputText}" AcceptsReturn="True"/>
                <custom:CustomButton Grid.Column="1" Style="{StaticResource CustomButtonStyle}" Width="50" Height="50" Command="{Binding SendCommand}" Icon="/Icon/send.png" Margin="10,25,0,25"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
