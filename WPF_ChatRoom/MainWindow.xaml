<Window x:Class="WPF_ChatRoom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_ChatRoom"
        xmlns:custom="clr-namespace:WPF_ChatRoom.CustomControl"
        Background="Transparent" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize" Height="720" Width="1280">
    
    <Border Background="Orange" BorderBrush="Black" BorderThickness="1" Margin="10" CornerRadius="20" MouseDown="UIElement_OnMouseDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!-- Meun -->

            <Border Grid.RowSpan="99" Margin="10,0,0,0" Width="50" Height="650" CornerRadius="20" Background="Aqua">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <custom:CustomButton Icon="/Icon/chat.png" Command="{Binding  HomePage}" Width="50" Height="50" Background="Transparent" Style="{StaticResource CustomButtonStyle}"/>
                    <custom:CustomButton Grid.Row="1" Icon="/Icon/setting.png" Command="{Binding  SettingsPage}" Width="50" Height="50" Background="Transparent" Style="{StaticResource CustomButtonStyle}"/>
                    <custom:CustomButton Grid.Row="2" Icon="/Icon/database.png" Command="{Binding  DataBasePage}" Width="50" Height="50" Background="Transparent" Style="{StaticResource CustomButtonStyle}"/>
                </Grid>
            </Border>
            
            <!--main-->
            <Border Grid.Column="1" Grid.RowSpan="99" Margin="10" CornerRadius="20" Background="White">
                <ContentControl Content="{Binding CurrentPage}"/>
            </Border>
        </Grid>


    </Border>
</Window>
